<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <AgoraPackageName>Extensions.Ains</AgoraPackageName>
    <!--
      A dynamic framework, so it cannot be embedded into this assembly and linked from there — the
      consuming app's linker never sees it. It travels beside the assembly instead, as
      <Assembly>.resources.zip.
    -->
    <NoBindingEmbedding>true</NoBindingEmbedding>
    <!--
      No VersionPrefix override: the extensions ride the RTC version line, which is this
      repository's default. Agora ships them inside the AgoraRtcEngine_macOS archive itself, at the
      same version as AgoraRtcKit.
    -->
  </PropertyGroup>

  <Import Project="../Agora.Binding.props" />

  <Target Name="ValidateAgoraXcframeworks" BeforeTargets="Build;Pack"
          Condition="!Exists('$(MSBuildProjectDirectory)/lib/AgoraAiNoiseSuppressionExtension.xcframework')">
    <Error Text="lib/AgoraAiNoiseSuppressionExtension.xcframework is missing. Run build/fetch-extensions.sh first." />
  </Target>

  <!--
    Empty on purpose — see src/Agora.Extension.md. This package carries native payload and no
    managed API: the switch that turns the extension on already exists on AgoraRtcEngineKit, in
    Net.Agora.Video.Mac / Net.Agora.Voice.Mac. The file exists at all because the iOS SDK refuses
    to build a binding project without one ("No API definition file specified").
  -->
  <ItemGroup>
    <ObjcBindingApiDefinition Include="ApiDefinition.cs" />
  </ItemGroup>

  <ItemGroup>
    <NativeReference Include="lib/AgoraAiNoiseSuppressionExtension.xcframework">
      <Kind>Framework</Kind>
    </NativeReference>
  </ItemGroup>

  <PropertyGroup>
    <Title>The Agora AI noise suppression extension for .NET for macOS.</Title>
    <Description>The native payload for Agora's AI noise suppression extension, staged out of the AgoraRtcEngine_macOS $(AgoraVideoMacVersion) archive and packaged for .NET for macOS. Removes background noise from the captured microphone stream with a neural model, well beyond what the engine's built-in suppressor manages in a noisy room. Add it alongside Net.Agora.Video.Mac or Net.Agora.Voice.Mac — it carries no managed API of its own; the switch that turns it on is AgoraRtcEngineKit.SetAINSMode in the RTC binding.</Description>
    <PackageTags>$(PackageTags);RTC;extension;AI;noise;suppression</PackageTags>
  </PropertyGroup>

</Project>
