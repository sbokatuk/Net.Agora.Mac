name: pull request

on:
  pull_request:
    types: [opened, synchronize, reopened]

concurrency:
  group: pr-${{ github.event.number }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  version:
    name: resolve beta suffix
    runs-on: ubuntu-latest
    outputs:
      suffix: ${{ steps.resolve.outputs.suffix }}
    steps:
      - id: resolve
        run: |
          suffix="beta.${{ github.event.number }}.${{ github.run_number }}"
          echo "suffix=${suffix}" >> "$GITHUB_OUTPUT"
          echo "Building every package at its own version with suffix -${suffix}" >> "$GITHUB_STEP_SUMMARY"

  build:
    name: build
    needs: version
    uses: ./.github/workflows/build.yml
    with:
      suffix: ${{ needs.version.outputs.suffix }}

  publish:
    name: publish beta to nuget.org
    needs: [version, build]
    runs-on: ubuntu-latest
    if: github.event.pull_request.head.repo.full_name == github.repository
    environment: nuget.org
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Set up .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 9.0.x

      - name: Download packages
        uses: actions/download-artifact@v4
        with:
          name: nuget-packages
          path: artifacts

      - name: Authenticate to nuget.org
        id: nuget-login
        uses: NuGet/login@v1
        with:
          user: ${{ secrets.NUGET_USER }}

      - name: Push to nuget.org
        run: |
          dotnet nuget push "artifacts/*.nupkg" \
            --source https://api.nuget.org/v3/index.json \
            --api-key "${{ steps.nuget-login.outputs.NUGET_API_KEY }}" \
            --skip-duplicate

      - uses: actions/checkout@v4

      - name: Summarise
        run: |
          . ./build/pins.sh
          s="${{ needs.version.outputs.suffix }}"
          {
            echo "### Published beta packages to nuget.org"
            echo "- [Net.Agora.Video.Mac](https://www.nuget.org/packages/Net.Agora.Video.Mac/${AGORA_VIDEO_PACKAGE_VERSION}-${s})"
            echo "- [Net.Agora.Voice.Mac](https://www.nuget.org/packages/Net.Agora.Voice.Mac/${AGORA_VOICE_PACKAGE_VERSION}-${s})"
            echo "- [Net.Agora.Signaling.Mac](https://www.nuget.org/packages/Net.Agora.Signaling.Mac/${AGORA_SIGNALING_PACKAGE_VERSION}-${s})"
          } >> "$GITHUB_STEP_SUMMARY"
